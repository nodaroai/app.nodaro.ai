name: Community E2E

# Boots the ACTUAL community edition — the published image, the published
# compose file, six containers, real Postgres + GoTrue + PostgREST + MinIO +
# Redis — and runs a real path through it: sign up, create a workflow, submit a
# generation, watch the job land.
#
# Why this exists: cloud is the only configuration we run every day, and
# community is defined by SUBTRACTION (no provider keys, no credits, no admin,
# no publicly-reachable media host). Every other job in CI tests the code;
# `backend-boot-smoke` proves the process starts. Neither can see a job that
# enqueues and then sits `pending` forever, a raw vendor 401 rendered into a
# node card, or discovery advertising a node this edition cannot run — the bugs
# that actually reached self-hosters. Those only appear when the whole stack is
# up with nothing configured, which is exactly what this job stands up.
#
# It runs against ZERO provider keys ON PURPOSE. The keyless install is the
# first thing every new self-hoster has, and the state we shipped the most bugs
# into.
#
# Deliberately secret-free (compose defaults only), so it also runs on fork PRs
# from the public mirror.
#
# SECOND LANE (#648): after the keyless contracts pass, internal pushes and
# manual dispatches ALSO walk the bring-your-own-key SUCCESS path — the
# configuration most self-hosters actually run (their own KIE key), the
# quickstart's documented happy path. The probe PASTES the key through
# /v1/setup/provider-keys (the in-app path — encrypted at rest, no .env, no
# restart), waits for the worker's refresh, and submits one REAL Z-Image
# generation (cheapest model, under a cent of provider spend per run), follows
# it to `completed`, and asserts the media landed in the install's own MinIO.
# That is hot registration end to end. This lane needs the
# COMMUNITY_E2E_KIE_KEY secret (a low-budget kie.ai key); when the secret is
# absent — every fork, and this repo until someone configures it — the lane
# no-ops and the job stays green on the keyless contracts alone. It never runs
# on pull_request, so PR runs stay fully secret-free and spend nothing.

on:
  pull_request:
    paths:
      - "Dockerfile"
      - ".dockerignore"
      - "docker-compose.community.yml"
      - "frontend/Caddyfile"
      - "backend/**"
      - "frontend/**"
      - "packages/**"
      - "supabase/migrations/**"
      - "tools/community-smoke.mjs"
      - ".github/workflows/community-e2e.yml"
  push:
    branches: [dev, main]
    paths:
      - "Dockerfile"
      - ".dockerignore"
      - "docker-compose.community.yml"
      - "frontend/Caddyfile"
      - "backend/**"
      - "frontend/**"
      - "packages/**"
      - "supabase/migrations/**"
      - "tools/community-smoke.mjs"
      - ".github/workflows/community-e2e.yml"
  workflow_dispatch: {}
  # Nightly, against the PUBLISHED image (ghcr…:latest) — the artifact
  # self-hosters actually run. Push/PR runs build from source; nothing watched
  # the published image at rest (a provider change, an expired dependency, a
  # broken mirror) until release check 53 (2026-08-16). Internal repo only,
  # where the alert issue lands. 04:23 UTC = an hour after the nightly mirror.
  schedule:
    - cron: '23 4 * * *'

# One run per ref — the stack is heavy and a superseded run tells us nothing.
concurrency:
  group: community-e2e-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  issues: write

jobs:
  e2e:
    name: Community Edition E2E
    # Pull requests run everywhere — including forks on the public mirror, which
    # is the point of keeping this job secret-free. Pushes only run on internal:
    # the mirror's main/dev are fast-forwards of commits this job already
    # cleared here, so building the image again there is ~20 minutes of runner
    # for zero added signal.
    if: github.event_name != 'push' || github.repository == 'nodaroai/app.nodaro.ai-internal'
    runs-on: ubuntu-latest
    # The first run on a cold buildx cache compiles the whole image (frontend +
    # backend + pinned ffmpeg); cached runs are far quicker. Sized for the cold
    # case so a cache miss is not read as a hang.
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v5
        with:
          persist-credentials: false

      - uses: docker/setup-buildx-action@v3

      # Single-arch and loaded into the local daemon — this is a test artifact,
      # not a release. The multi-arch push-by-digest build lives in
      # community-image.yml and runs only on the public mirror.
      - name: Build the community image
        if: github.event_name != 'schedule'
        uses: docker/build-push-action@v6
        with:
          context: .
          load: true
          tags: nodaro-community:ci
          build-args: |
            VITE_API_URL=http://localhost:3000
            VITE_EDITION=community
            VITE_SUPABASE_URL=http://localhost:3000/supabase
            VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJyb2xlIjoiYW5vbiIsImlzcyI6InN1cGFiYXNlIiwiaWF0IjoxNzU0Nzg0MDAwLCJleHAiOjIwNzAxNDQwMDB9.Z1_aDD7opHbwv6f8yrHK0ptlIM2tg1ulhXsggLuDyqo
          cache-from: type=gha,scope=community-e2e
          cache-to: type=gha,mode=max,scope=community-e2e

      # The published compose file verbatim, so this tests the file users
      # actually run. --no-build pins it to the image built above; every other
      # value is the committed default, which is what a fresh install gets.
      # NODARO_CLOUD_URL points at an address that refuses instantly inside the
      # container. The probe's connect-start contract must exercise the
      # ERROR branch here: with the default (app.nodaro.ai) and
      # COMMUNITY_CONNECT_ENABLED now on in production, every CI run would
      # otherwise mint a real DCR client on the production cloud.
      - name: Pull the published community image (nightly)
        if: github.event_name == 'schedule'
        run: docker pull ghcr.io/nodaroai/nodaro-community:latest
      - name: Boot the community stack
        env:
          # Push/PR: the image built above. Nightly: the published one.
          NODARO_IMAGE: ${{ github.event_name == 'schedule' && 'ghcr.io/nodaroai/nodaro-community:latest' || 'nodaro-community:ci' }}
          NODARO_CLOUD_URL: http://127.0.0.1:9
        run: |
          docker compose -f docker-compose.community.yml up -d --wait --wait-timeout 420 --no-build

      - name: Run the community contract probe
        run: node tools/community-smoke.mjs http://localhost:3000 --strict-keyless

      # ── BYO-key success lane (#648) ──────────────────────────────────────
      # Gate first: internal pushes/dispatches only (never pull_request — PR
      # runs must stay secret-free), and only when the low-budget key secret
      # exists. The secret is referenced ONLY in the gate and the keyed boot
      # below — job-wide env would leak it into the KEYLESS boot above via
      # compose's ${KIE_API_KEY:-} passthrough and fail --strict-keyless.
      - name: BYO-key lane gate
        id: keyed_gate
        if: github.event_name != 'pull_request' && github.repository == 'nodaroai/app.nodaro.ai-internal'
        env:
          KIE_API_KEY: ${{ secrets.COMMUNITY_E2E_KIE_KEY }}
        run: |
          if [ -n "$KIE_API_KEY" ]; then
            echo "run=true" >> "$GITHUB_OUTPUT"
          else
            echo "run=false" >> "$GITHUB_OUTPUT"
            echo "::notice::COMMUNITY_E2E_KIE_KEY is not configured — BYO-key lane skipped"
          fi

      # One real generation, the way the quickstart now tells users to do it:
      # paste the key on /setup (the probe PUTs it to /v1/setup/provider-keys
      # — the in-app path, encrypted at rest) and hit Run — NO restart. The
      # worker has to pick the key up on its refresh interval, so this proves
      # hot registration end to end against the same DB/Redis/MinIO the
      # keyless lane just exercised. The key reaches the probe process only,
      # never compose (a compose passthrough would fail --strict-keyless).
      - name: Run the BYO-key success probe (key pasted through /setup, no restart)
        if: steps.keyed_gate.outputs.run == 'true'
        env:
          SMOKE_KIE_API_KEY: ${{ secrets.COMMUNITY_E2E_KIE_KEY }}
        run: node tools/community-smoke.mjs http://localhost:3000 --keyed

      # Without this a failure is a red X and nothing else — the stack is gone
      # by the time anyone looks.
      - name: Dump stack logs on failure
        if: failure()
        run: |
          docker compose -f docker-compose.community.yml ps
          docker compose -f docker-compose.community.yml logs --no-color --tail 400

      # Nightly only: a broken PUBLISHED image must not fail silently (the
      # same one-issue-per-outage pattern as mirror-to-public).
      - name: Alert on nightly failure (auto-issue)
        if: failure() && github.event_name == 'schedule' && github.repository == 'nodaroai/app.nodaro.ai-internal'
        env:
          GH_TOKEN: ${{ github.token }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: |
          set -euo pipefail
          gh label create community-image-failure --repo "$GITHUB_REPOSITORY" --color B60205 --description "nightly community-e2e against the published image failed" --force 2>/dev/null || true
          OPEN=$(gh issue list --repo "$GITHUB_REPOSITORY" --label community-image-failure --state open --json number --jq '.[0].number // empty')
          if [ -n "$OPEN" ]; then
            gh issue comment "$OPEN" --repo "$GITHUB_REPOSITORY" --body "nightly community-e2e (published image) failed again: $RUN_URL"
          else
            BODY=$(printf 'The nightly community contract probe against the PUBLISHED image (ghcr.io/nodaroai/nodaro-community:latest) failed: %s\n\nThis is what self-hosters download today. Check the run log for the contract that broke; close this issue when a nightly run succeeds.' "$RUN_URL")
            gh issue create --repo "$GITHUB_REPOSITORY" --label community-image-failure --title "community-e2e: the published image fails the contract probe" --body "$BODY"
          fi

      - name: Tear down
        if: always()
        run: docker compose -f docker-compose.community.yml down -v
