/**
 * `@nodaro/studio-production` — the studio's production document, as code.
 *
 * The barrel grows one cluster at a time as the codec moves in from the studio
 * app (the codec, the plan format, the bundle, then the operations, the request
 * builders and the view). Everything exported here runs under plain node.
 */
export {}
