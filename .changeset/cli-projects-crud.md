---
"@nodaro/cli": minor
---

Add `nodaro projects create`, `nodaro projects update`, and `nodaro projects delete` subcommands. Projects now have full CRUD from the CLI, matching the `@nodaro/client` `projects` resource.
