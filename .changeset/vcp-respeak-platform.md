---
"@nodaro/sdk": minor
"@nodaro/cli": minor
---

Voice Changer Pro: per-voice `engine` — `"sts"` (default recast) or `"v3"` (Re-speak: the performance is regenerated from the transcript with eleven_v3, `[audio tags]` supported). Analyze results now carry per-segment transcript `text` (the paid input for a v3 speaker, editable before conversion). Requires a platform running cloud-plugins >= 0.216.0; on older platforms the fields are accepted-and-ignored.
