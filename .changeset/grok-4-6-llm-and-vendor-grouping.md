---
"@nodaro/shared": minor
---

Add the `grok-4.6` LLM (KIE responses dialect on the grok family path, vendor `xai`, standard tier, vision + structured output + reasoning efforts low–xhigh, live-verified 2026-08-18) and the vendor-grouping helpers every LLM model menu now renders from: `LLM_VENDOR_ORDER`, `LLM_VENDOR_LABELS`, `groupLlmModelsByVendor`, `orderedLlmModels`, plus the `LlmVendor`/`LlmModelGroup` types.
