---
"@nodaro/client": minor
---

Add the `community` resource: browse, get, favorites, clone, favorite, and report shared characters/locations/objects. (Publishing is admin-only via the editor and is intentionally not exposed in the SDK.)
