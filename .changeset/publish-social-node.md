---
"@nodaro/shared": patch
---

Add `publish-social` to `SOCIAL_POST_NODE_TYPES` — the unified Publish-to-Social node's type, so shared-set-driven routing (carousel accumulation, caption, refMap gate) covers it in both the frontend DAG executor and the backend orchestrator.
