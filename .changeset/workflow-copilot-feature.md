---
"@nodaro/shared": minor
---

Add `workflow-copilot` to the `LlmFeature` union and its default model, so the in-app Workflow Copilot's turns resolve a model and a credit identifier the same way every other LLM feature does.
