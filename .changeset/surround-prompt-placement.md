---
"@nodaro/shared": minor
---

The surround fill-prompt builder moves out of this package. `SURROUND_DIRECTIONS`, the carried-fraction defaults, `isTiltDirection`, and `defaultCarriedFraction` are unchanged; `buildSurroundFillPrompt` is no longer exported (it was never part of the SDK surface).
