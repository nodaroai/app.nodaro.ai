---
"@nodaro/client": minor
---

voices.recast(): per-voice recast settings (stability/similarity/style/speakerBoost/seed/volumeMode/volume) + separationQuality + node-level voiceFx (reverb/echo applied to the combined recast voices before the background is mixed back) + node-level musicVolumeMode / musicVolume (level of the preserved background music — match / normalize / manual %)
