---
"@nodaro/shared": minor
---

Add `TELEGRAM_ACCOUNT_TRIGGER_NODE_TYPE` (`"telegram-account-trigger"`) to `PROJECTED_TRIGGER_NODE_TYPES`: a workflow saved with that node now has its trigger row projected like the schedule, webhook and bot-trigger nodes.
