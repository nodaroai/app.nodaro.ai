---
"@nodaro/sdk": minor
---

Add `client.catalogs.list()` + `GET /v1/catalogs` — the tag-free, pack-composed catalog projection. Reflects the deployment's registered vendored packs so thin clients rendering their own pickers honor a deployment's curation.
