---
"@nodaro/shared": minor
---

video-analysis: `onScreenTextKind` on scenes, clip-level `transitionIn`, and the `VIDEO_ANALYSIS_TEXT_KINDS` / `VIDEO_ANALYSIS_CLIP_TRANSITIONS_IN` vocabularies (keys only — no existing enum widened).
