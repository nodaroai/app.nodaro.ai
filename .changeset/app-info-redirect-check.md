---
"@nodaro/sdk": minor
---

`oauth.getAppInfo(clientId, redirectUri?)` — pass the redirect URI and the answer carries `redirectUriRegistered`, so a consent screen can refuse to redirect anywhere unregistered.
