---
"@nodaro/shared": minor
---

Add `SUNO_TRACK_SOURCE_TYPES` — the node types whose output carries Suno chaining ids (`sunoTrackId` / `sunoTaskId`), shared by the canvas resolver, the orchestrator resolver and the editor's "Inherited" hint.
