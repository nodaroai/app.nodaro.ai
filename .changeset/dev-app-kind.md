---
"@nodaro/sdk": minor
---

`DeveloperApp.kind` — how the app came to exist (`user`, `dynamic_mcp`, `first_party_mcp`, `community_instance`). Only `user` apps count toward the per-user registration cap.
