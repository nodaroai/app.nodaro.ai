---
"@nodaro/shared": minor
---

Export `OVERLAY_ANCHORS` / `OverlayAnchor` — the nine points an Image Overlay layer attaches to. One definition for the route's validation, the compositor, the canvas, the SDK and the CLI (it used to be spelled out separately in each).
