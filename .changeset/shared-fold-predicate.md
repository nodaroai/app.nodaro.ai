---
"@nodaro/shared": minor
---

`videoProviderFoldsLoneEndFrame(provider)` — the models whose worker folds a lone last frame into the references (Seedance 2.x / MiniMax Hailuo 3 / Wan 3). Exported so a client can gate its own end-frame-only run the way `/v1/generate-video` does, instead of hand-mirroring the family list
