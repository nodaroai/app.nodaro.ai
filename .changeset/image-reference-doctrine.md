---
"@nodaro/prompts": patch
---

New `IMAGE_REFERENCE_PROMPT_DOCTRINE` — the `{image:N:label}` reference-token idiom for image generation (token grammar, connection-order numbering, composition patterns), rendered into the generate-image node skill by gen-skills the same way the per-provider video doctrines are.
