.claude/
!.claude/skills/nodaro-film-director/
.superpowers/
CLAUDE.md
!/CLAUDE.md
!frontend/CLAUDE.md
!backend/CLAUDE.md
!backend/src/providers/kie/CLAUDE.md
!backend/src/providers/replicate/CLAUDE.md
/ARCHITECTURE.md
architecture-graph.html
node_modules/
# This repo uses npm (single root package-lock.json). A stray pnpm-lock.yaml
# or bun.lock only creates a second, drifting dependency graph that security
# scanners flag for CVEs the npm build never installs (e.g. a phantom Next.js
# pin, or a stale `ws` in packages/remotion/bun.lock). Keep them out.
pnpm-lock.yaml
bun.lock
.next/
.env
.env.*
!.env.example
!.env.community.example
__pycache__/
*.pyc
.pytest_cache/
.coverage
htmlcov/
dist/
build/
*.egg-info/
.venv/
venv/
coverage/
.DS_Store
.playwright-mcp/
supabase/.temp/

# Scraped pages / scratch dumps — never commit (see OSS-readiness audit)
# removed
# removed
# removed
/gen.txt

# Merge / patch backup artifacts (never commit these)
*.orig
*.rej

# Provider-billing analysis artifacts (backend/scripts/*.mts inputs+outputs).
# These are REAL production billing extracts — per-task provider ids, per-model
# volume, and our provider unit rate. Generate them locally when auditing; they
# must never be committed (backend/kie-per-task.json shipped in every release
# tarball from 2026-07-30 until this rule). check-billing-dumps.mjs enforces it.
kie-per-task.json
kie-actual.json
per-second-rates.json
observed-costs.json
a2-sheet.json

# Credential files — a belt-and-braces net beside secret-scan.yml/gitleaks.
# The SAI fork added these after finding a `kie-key.txt` sitting at its repo
# root: a provider key in a file gitleaks does not recognise as a secret (it is
# bare, with no assignment or JSON key around it) is one `git add -A` from
# public history. Globs cost nothing and fail closed.
# NOTE: *.key would also match TLS/SSH private keys — which is the point.
*-key.txt
*_key.txt
*.key
*.pem
*credentials*.json
